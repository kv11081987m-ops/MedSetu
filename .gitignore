# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — never commit these
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Database backups — contains real customer/order/prescription data,
# must never reach git. See scripts/backup.md.
backups/

# Supabase CLI local link/cache state — machine-specific, regenerated by
# `supabase link`, not meant to be committed (function code itself, under
# supabase/functions/, is still tracked normally).
supabase/.temp/
supabase/.branches/
